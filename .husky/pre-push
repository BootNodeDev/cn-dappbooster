set -e

echo "TypeScript checks: carpincho-wallet"
( cd carpincho-wallet && npx tsc -b --noEmit --pretty )

echo "TypeScript checks: counter/frontend"
( cd counter/frontend && npx tsc -b --noEmit --pretty )

echo "TypeScript checks: canton-base/wallet-service"
( cd canton-base/wallet-service && npx tsc --noEmit --pretty )

echo "TypeScript checks: canton-connect-kit"
( cd canton-connect-kit && npx tsc -b --noEmit --pretty )

echo "All TypeScript checks passed. Proceeding with push."
