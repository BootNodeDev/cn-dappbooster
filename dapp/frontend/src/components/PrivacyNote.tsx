// Shown when the acting party sees nothing — the store's party partition yields an
// empty view because the ledger only discloses what you're a stakeholder of. The
// net-new point of the direct effort: privacy is real, not simulated.
export const PrivacyNote = (): React.JSX.Element => (
  <p className="mt-4 text-center font-mono text-[0.7rem] text-fg-soft">
    privacy holds — you see only what you're party to
  </p>
)
