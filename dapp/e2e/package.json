{
  "name": "@canton-dappbooster/e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Integration tests across the canton-barebones, wallet-gateway-devkit, carpincho-wallet, and dapp/frontend packages. Black-box: consumes each package via its stable surface (HTTP endpoints, extension artifact, dApp UI). No direct source imports.",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "lint": "biome check",
    "lint:fix": "biome check --write",
    "format": "biome format --write",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report",
    "install:browser": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.1",
    "@types/node": "^25.0.2",
    "typescript": "^5.9.3"
  }
}
